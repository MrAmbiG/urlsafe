apiVersion: v1
kind: Service
metadata:
  name: {{ include "urlsafe.fullname" . }}
  labels:
    {{- include "urlsafe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "urlsafe.selectorLabels" . | nindent 4 }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
